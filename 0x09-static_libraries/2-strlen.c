#include "main.h"
/**
 * _strlen - function that returns the lengh of a string
 * @s: input
 * Return: lengh of the string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
